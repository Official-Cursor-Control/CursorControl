# Profile font assets

Binary font files are not included in this distributed source archive.
The packaged Windows runtime already contains the required font data in the executable.
For a local source rebuild of the advanced profile-font feature, provide appropriately licensed local font files matching the filenames referenced by `advanced_profile_text.go`.
