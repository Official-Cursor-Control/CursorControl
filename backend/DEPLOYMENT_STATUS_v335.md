# v335 backend deployment status

- `player-profile` Edge Function deployed as version 6.
- New profile-skin IDs are 101..111; legacy frame IDs 1..7 are filtered from the new skin collection.
- `unlocked_profile_frames` and `selected_profile_frame` continue to be the cross-device persistence fields.
